# 逆道西行 · 版本更新记录

> 本文件记录《逆道西行》项目的版本更新历史，每次代码变更后必须在此文件中备注更新内容。
> 版本号对应 `demo/index.html` 中的 `?v=NN` 缓存版本号。

## 版本更新记录

### 2026-09-12（续）劫印品质阶梯重构（版本戳：jieseals 389 · game_rest 445 · ui_panel_2 503）
> 本节为同日「劫印系统完善」之后的第二轮专项，取代其上「红劫掷骰升华」口径（开发文档对应 § 已加 ⚠️ 作废指引）。
**更新内容**：重构劫印品质阶梯为 `白<蓝<红<金`，红劫=融合3难/Boss 双源保底、金劫=顶阶仅由三红合金合成，层级干净、合金价值在「构筑精简+顶阶标识」而非数值暴增。
- **阶梯翻转（用户拍板）**：原提案「两段蓝/三段红/三红合金」在旧 `白<蓝<金<红` 下隐含层级倒挂（红比金强、金易得反弱）与合金亏值（3红合1金蒸发78%战力）；拍板 **Boss 掉落改红劫**（金劫不再由来源直给）+ **金=3×红**，使合金功率中性。
- **红劫双源保底**（`jieseals.js`）：`SEAL_SOURCE_TIER.fusion` 删 `redBase/redPerUp/redCap`，仅留 `byFusion`；`rollSealTier` 融合分支 `n>=3` 直接 `return 'red'`（不掷骰）；`trial.bossTier gold→red`。trial/elite/good/xinmo 及融合<3 恒不产 red。
- **金劫唯一来源=三红合金**（`jieseals.js`/`game_rest.js`/`ui_panel_2.js`）：新增 `NDX.combineRedSeals`（消耗 3 红→1 金、道途取多数派）；土地庙 rest 面板新增 `alloy` 动作与合金按钮（红劫≥3 显示）；`rollSealTier` 任何来源恒不产 gold（门禁锁定）。
- **真 bug 修复**（`jieseals.js`）：红劫补档原 `Math.round(gold×1.55×100)/100` 四舍五入破坏「金≡3×红」精确不变量（红 0.53 vs 金 1.581，差 0.009）→ 去四舍五入后数学严格相等；显示层 `Math.round(val×100)%` 已自行取整，玩家视觉无影响。
- **数值/计层单一真源**：`SEAL_RED_MULT=1.55`、`SEAL_GOLD_SCALE=4.65(=1.55×3)`、`sealTierVal` 重写；`sealLayerVal` 金=3/红=2；`SEAL_TIER_GOLD` 白6/蓝12/红30/金45（按战力序）。
- **门禁**：`_verify_seal_source.js` 更新 B/B2 断言（红劫双源保底＋金劫来源唯一＋金=红×3 倍率）；`_verify_seal_pool.js` 更新 F/G/H（计入阶段碑／金3层／战力序定价）；新增 `_verify_seal_alloy.js`（倍率不变量／不足三红拒绝／多数派／功率中性／词表／UI落位）。`_run_all_gates.js` **19/19 worst=0**。
- **版本戳**：jieseals 388→389、game_rest 444→445、ui_panel_2 502→503；`node --check` 全通过；EOL 守恒。
- 开发文档「劫印系统完善 §二」加 ⚠️ 作废指引，新增「劫印品质阶梯重构」续节；报告 `docs/nidao-xiyou-report/nidao-xiyou-seal-tier-ladder-2026-09-12.html`。

### 2026-09-12 劫印系统完善（版本戳：jieseals 387 · attr_calc 445 · game_camp 445 · game_compound 428 · game_event_1 437 · main 501）
> 说明：本节不再以单一 `v=NN` 编号——自 V8.44 起各文件版本戳已独立递增（当前分布于 386~501），以文件戳为准。
**更新内容**：以只读体检探针 `scripts/_audit_seal.js` 为入口，收掉劫印系统三处「有数据、无通路」缺陷，并新增 2 道门禁（16 → 18 全绿）。
- **红劫（red）落为「三难融合专属」**（`jieseals.js`）：`SEAL_SOURCE_TIER.fusion` 增 `redBase 0.10 / redPerUp 0.03 / redCap 0.30`；融合子难 0/1→白（不掷骰）、2→蓝（不掷骰）、**3→金为底，按逆道抉择深度掷一次升华骰判红**（判据严格小于）；trial/elite/good/xinmo 恒不产 red。修掉「合成链被砍后红劫无来源」的断链（此前 47/47 词条已备 red 数值 ×1.55，仅缺来源）。数值 [PLACEHOLDER · 待10局采样]。
- **篝火仪典三段接线**（`jieseals.js`/`game_camp.js`/`main.js`/`game_event_1.js`）：`BONFIRE_RITES` 改**声明式单一真源**（每项自带 `costOk/costWhy/costFn/pay/gain/gainText`），新增统一执行器 `NDX.doRite(s,id)` 与 `NDX._bonusTi`；`riteList` 补齐四项资源门控与不足原因；`chooseRite` 改走 `doRite`；main.js rite 分发由 1 个 case 扩为 4 个（`action.slice(5)`）；`bonusTi` 初始化补 `mdef`。修复 life/xinmo/incense **点击完全无响应**（原仅 blood 有执行）。
- **仪典文案归真**（删两处幽灵效果）：「+3% 诵经伤害」（全仓无结算字段）→「+2% 暴击」；「心魔上限 +5」（需改 `XINMO.MAX` 内核常量）→ 去除（其代价本就是消耗的 10 点心魔）。
- **白档废印修复**（`jieseals.js`）：`offerSealsAligned` 补品阶数值过滤，与 `offerSeals` 口径统一——修复前白档会产出 `val=undefined` 的零收益废印（逐杀/焚天/金蝉/万象/逆鳞）。反证实测：修复前 326 次 → 修复后 0 次。
- **并行假真源修正**（`attr_calc.js`）：`calcSealBonus` 原按不存在字段名（`maxHp/cri/lifeSteal`）直读 seal 对象、对劫印恒返回 `{}`；改为 `stat → 目标字段` 真源口径映射，并注明战斗数值真源唯一在 `combat.js computeStats`。
- **门禁**：新增 `scripts/_verify_seal_rites.js` **53/0**（数据契约/三段接线/结算/8 组门控边界/结构性守卫/兼容/文案）、`scripts/_verify_seal_pool.js` **42/0**（4000 次白档采样 + 反证断言 + 图标双向一致 + calcSealBonus 口径 + 阶段碑 + 档位词表）；`_verify_seal_source.js` 扩充至 **55/0**（融合掷骰契约更新 + 红劫 9 条）。`_run_all_gates.js` **18/18 worst=0**。
- **版本戳**：上述 6 个文件递增；`node --check` 全通过；EOL 守恒（5 文件纯 CRLF、main.js 纯 LF）。
- 开发文档新增「劫印系统完善（2026-09-12）」章节；报告 `docs/nidao-xiyou-report/nidao-xiyou-seal-complete-2026-09-12.html`。

### v=447（2026-09-06）中后段地区（10-17）文案厚度 + 地图皮肤 + 幕布地区开场叙事
**更新内容**：审计中后段地区（祭赛/比丘/天竺等 10-17 地区）叙事与地图表现，补齐地区开场叙事（幕布展示）、关键难目暗叙扩写、以及按地区主题化的地图皮肤；回归门禁新增 J 段专项校验。
- **地区开场叙事**（`data_regions.js`/`ui_trial.js`/`style.css`）：`ACT_MAP_THEME` 新增 `regionIntro` 字段（已为 12 祭赛国/14 比丘国/15 天竺·玉兔 落地，73/85/76 字），进入该地区首难时幕布附 `.tc-region` 楷体地区开场文案，不与既有难目标题争空间。
- **关键难目暗叙扩写**（`trials81.js`）：难 51 金光寺冤僧（祭赛）、难 54 狮驼岭（苦海）、难 59 比丘白鹿（比丘）、难 68 玉兔假公主（天竺）、难 72 玉兔遁月 等暗叙/开场双备，字数门槛 ≥40；聚焦难目（51/54/59/68/72）暗叙 ≥60-80 字，突出「住持不认、小沙弥不认」「鹿眼藏在官帽下」「它偷的是那三百年没被人好好看过的自己」等原著暗面。
- **地图皮肤**（`ui_map.js`/`style.css`）：`#mapArea` 按地区挂 `skin-act-10`~`skin-act-17` 类，连线/节点名/图标光晕随地区主题变色——10 幽冥真假紫、11 火焰山橙、12 祭赛佛光金、13 狮驼铁锈棕、14 比丘药炉青、15 天竺月宫银蓝、16 灵山鎏金、17 凌云渡水青白；1-9 地区保持默认样式，皮肤零 JS 运行时开销。
- **门禁**：`regression.js` 新增 J 段「中后段地区文案厚度 + 地图皮肤数据」4 项（地区 10-17 主题齐备 names≥actLayers、12/14/15 regionIntro ≥40 字、10-17 全难 dark/intro 双备、难 51/54/59/68/72 深度达标）；全套回归 **157/0**、26 组冒烟测试全绿。
- **版本戳**：style.css、data_regions.js、trials81.js、ui_map.js、ui_trial.js 递增至 v=447；同步修正 data_reincarnation.js/data_sutra.js 版本戳至 v=446（对齐 v=446 记录）。
- 开发文档新增 §4.10「中后段地区叙事与地图皮肤（10-17）」；补充 §地图渲染 地区皮肤与幕布开场口径。

### v=446（2026-09-06）V3 死亡增强改造落地 · P0-C 去纵向 + 横向渐进解锁
**更新内容**：按《逆道西行_V3思路重析_各系统差距与建议_2026-09-05.md》§三 完成死亡增强改造——轮回赐福/劫灰坊「去纵向攻血加成」，改为以「累计死亡次数」为轴的横向渐进解锁（传承英雄/传承经文/传承事件），失败一局＝一桩可解锁的新内容，缓解「挫败-变强」死循环。
- **劫灰坊去纵向**（`data_reincarnation.js`）：`ASH_SHOP` 金蝉余韵(hp)/逆心初萌(dmg) 由「全局 +%气血/伤害（max 10 级）」改为「横向——每级解锁一名传承英雄」（max 2，`ashUnlockHeroCount` 至多 4 名）；六道亲和(roll)/法宝共鸣(chg)/劫印拓印(seal) 属工具/解锁型保留纵向。`applyPostProcessing` 攻血%、`_ashBattleFlags` dmgMul 纵向乘法全部移除。
- **轮回赐福去纵向**（`data_reincarnation.js` `favorBonus`）：bonusTi/bonusYuan 恒为 0，不再提供任何乘法攻/血/减伤/法强/法抗；仅保留「感受性保底」——每 3 次失败 +1 掉装 soft-pity 提前层（≤3）与起始金（每 5 点 +5），诚实不越线。
- **死亡渐进解锁表**（`data_reincarnation.js` `DEATH_UNLOCKS`）：四类清册 heroes/sutras/events/zhuanjie；英雄解锁阈值 1/2/4/8（悟空/八戒/沙僧/小白龙），经文/事件阈值 3/6。API：`deathCount`/`bumpDeathCount`/`deathUnlockedOf`/`getDeathUnlocks`/`getNextDeathUnlock`/`resetDeathUnlocks`；英雄形成「通关全解锁 / 劫灰买印逐解 / 死亡递进逐解」三通道（`isHeroUnlocked` 合并判定）。
- **传承经文**（`data_sutra.js`）：新增《观音经》（deathReq 3，气血自愈同涨，持诵 zen-heal 闻声救苦）与《文殊般若经》（deathReq 6，体攻暴击并明，持诵 war-buff 慧剑断执）；`SUTRA_DEATH_POOL` 不进常规地区池，`sutraDropChoices` 累计死亡达阈值才并入渡经候选池；`synergyInReach` 过滤未解锁传承经；`SUTRA_DAO_TAG` 登记（观音→渡、文殊→战）。
- **传承事件**（`events.js`）：新增「观音·化缘旧事」（region 3–8，deathReq 3）与「文殊·慧剑遗痕」（region 8–13，deathReq 6），渡口/山崖文本各带渡经或金两选项；`sutraEventPool` 按 deathReq 过滤，未达阈值不进问号池。
- **门禁**：新增 `scripts/_smoke_death_unlock.js` **58/0**（劫灰横向线 max/unit、死亡渐进英雄 1/2/4/8、即将解锁清单、favorBonus 纵向恒 0 + 保底层/起始金、applyPostProcessing 满级 vs 0 级数值相等、传承经文/事件候选池动态并入与未达阈值排除、传承经已合不再入池、reset 保留工具线与劫灰币）。
- **版本戳**：data_reincarnation.js、data_sutra.js、events.js、data_favor.js、data_heroes_data.js、data_save.js、ui_misc_3.js 递增至 v=446。
- 开发文档新增 §4.9「死亡增强改造（V3 §三）」；§9.1 佛经补传承两经说明。

### v=445（2026-09-06）V3 经文系统改造落地 · P0-B 合成入背包 + 自动路由
**更新内容**：按《逆道西行_V3思路重析_各系统差距与建议_2026-09-05.md》§二 完成经文系统「合成入背包 + 道途标签 + 自动路由主道途生效」改造——集齐全本先入 `s.sutraBackpack`（待投），再按当前主道自动并入 `s.sutras`/`s.niSutras`（已投）生效；不新增玩家决策点，改道时补路由兜底。
- **背包与自动路由 API**（`data_sutra.js`）：新增 `sutraBackpackOf`（待投背包读写）/`routePendingSutras`（待投全本按主道自动并入生效，返回 `{routed, dao, pending}`）/`sutraRouteState`（路由状态查询）；待投期间不提供任何加成（`computeStats` 只读已投集合），改道经 ×1.5 判定由 `computeStats` 实时跟随主道。
- **合成/掉落链路改造**（`data_sutra.js`/`game_sutra.js`/`game_vault.js`）：`grantSutraShard`/`tryCombineNiSutra`/`grantNiSutraFrag`/`chooseSutra`/舍利塔拾经全部改为「合成后先入背包 → 立即 `routePendingSutras` 自动投入主道途」；`availableSutras`/`sutraDropChoices` 将待投全本视为已完成，不再可合成/不再进入掉落候选池（渡侧池补 `done` 过滤，修复待投经仍出现在三选一候选的问题）。
- **改道钩子**（`game_region.js`）：`chooseMainDao` 改道后自动路由待投经文至新主道，无待投时提示「道经相随」已投经随道心流转。
- **UI 层**（`ui_misc_4.js`/`ui_panel_2.js`/`style.css`）：经匣卡片显示「已投 · ×道（×1.5 主道加成）」/「待投」标签，顶部待投横幅提示改道即投入；佛经合成面板文案改为「经义自动投入主道途生效」，按钮标签「已合全本 · 已投主道」；新增 `.sx-invested-tag`/`.sx-pending-tag`/`.sx-pending-banner` 样式。
- **门禁**：`_smoke_sutra_route.js` 扩展至 **35/0**（T5 合成入背包立即路由、T6 逆经同链路、T7 候选池防重复含渡侧待投过滤、T8 改道无残留 + 渡经实时退回 ×1.0）；`_smoke_sutra_drop.js` 15/0 全绿。
- **版本戳**：data_sutra.js、game_sutra.js、game_region.js、game_vault.js、ui_misc_4.js、ui_panel_2.js、style.css 递增至 v=445。
- 开发文档新增 §4.8「经文系统改造（V3 §二）」；§九「经文系统」补待投/已投/自动路由口径说明。

### v=444（2026-09-06）V3 劫印砍管理落地 · P0-A 全量自动生效
**更新内容**：按《逆道西行_V3思路重析_各系统差距与建议_2026-09-05.md》§1.1 完成劫印系统「砍生效格/砍品质堆叠表」改造，劫印全量自动生效，品阶降级为稀有度标签、金=2 层/红=3 层计入道途层数。
- **砍生效格**（`jieseals.js`/`game_seal.js`/`ui_panel_2.js`/`ui_bag.js`）：删除 `sealSlotCap`/`toggleSealActive`/`activeSealsFor`/`sealActiveCount` 生效格管理函数与「捺存/换上」逻辑；`addSeal` 改为全量自动并入 state，仅保留唯一劫印去重；土地庙「劫印」面板改为「道途层数·阶段碑」（3/6/9/12 层自动触发六道阶段加成，`sealDaoLayerSum`/`sealBreakInfo`/`sealLayerVal` 承接）。
- **砍品质堆叠表**（`jieseals.js`/`game_seal.js`/`main.js`/`game_camp.js`）：铸印司三合升阶（白3→蓝/蓝3→金/金3→红）、2换1、同阶相易、红印易异、土地庙购印、重铸全部下线，品阶只随获取来源自然产生（白/蓝/金/红 仅作稀有度标签）。
- **层数计数等价**（`jieseals.js`/`combat.js`/`data_favor.js`）：金=2 层、红=3 层计入道途层数（`sealLayerVal`），道途进阶、技能进阶、心魔养恶、混元淬炼解锁（任一道途层数≥6）等判定全部改为按「全部持有劫印」累计，与动态主道判定（劫印≥3 且领先≥2）兼容。
- **全项目残留清理**（`dao_system.js`/`battle_resource.js`/`attr_calc.js`/`data_life.js`/`equipment.js`/`game_core_2.js`/`game_event_3.js`/`game_combat_2.js`）：三处 `sl.active !== false` 过滤改按全部持有印统计；删除 `SEAL_COMPOSE_COST`/`SEAL_SHOP_COST` 寿命配置与相关注释；观音/土地庙教学文案改写（劫印自动生效、无铸印司）；main.js 移除 9 个指向已删函数的死事件分支；ui_panel_2.js 浮层分类表清理 `seal-cast`/`seal-compose`/`seal-swap-pick` 等已下线 kind。
- **门禁**：31 组全绿（含 `_smoke_daoxin_sealmod` 48/48、`_smoke_sutra_drop` 15/0、`_seal_pity_smoke` 10/0、`_smoke_audit_resonance` 31/0、`_smoke_sutra_route` 15/0、`bus_decoupling_test`、`validate_audio`）；`scripts/_bg.js` 浏览器路径改为动态探测（EdgeCore 版本目录/Playwright Chromium 兜底），修复本机无标准 Edge 路径导致截图类脚本 ENOENT。
- **版本戳**：本次涉及 21 个 js 文件统一递增至 v=444（data_life/data_favor/data_sutra/equipment/jieseals/combat/battle_resource/attr_calc/dao_system/game_camp/game_combat_2/game_core_2/game_core_3/game_event_3/game_region/game_rest/game_seal/ui_bag/ui_modals_1/ui_panel_2/main）。
- **门禁套件加固（补充）**：全量门禁重跑 60 组 35 通过/25 失败，逐一修复后全绿——① `_refactor_verify.js` 结构检查新增 `KNOWN_REMOVED` 白名单（v=444 劫印砍管理 12 项 + 命痕/仪典 5 项有意删除，逐项确认零残留引用），校验口径改为「split 无缺失/无重复」（拆分后新增方法属正常演化）；② `_refactor_fix_loaders.js` 识别「已应用」目标形态不再误报失败；③ `_verify_reincarnate.js` 相对路径改 `path.join(__dirname,'..','js')` 修复跨 cwd ENOENT；④ 24 个遗留脚本（`_bg2/_chk/_corner/_debug_*/_http/_probe/_px/_scan/_verify2/_shot*/cdp_*`）硬编码 Edge Application 路径批量替换为动态解析（EdgeCore 版本目录探测 + 兜底），与 `_bg.js` 同模式，`_chk.js` 实跑浏览器拉起正常；⑤ `_verify_crit_burst`（合成 crit-burst 渲染 12 火花）/`_verify_pace_v853`（预输入不跳拍 overrideDelta=1）重跑转绿。开发文档 §4.3/4.4/4.6 归档旧铸印描述并新增 §4.7「劫印砍管理」。

### v=443（2026-09-05）肉鸽视角完善六连 · 击杀透明化/绝境反制/劫难词条/随从/满而不溢/释经弃印
**更新内容**：按《逆道西行_战斗系统_肉鸽视角完善分析_2026-09-05.md》落地全部 P0–P2 六批优化（五行方向已按要求放弃）。
- **P0-1 击杀判定与伤害预估（Balatro 式透明化）**（`combat.js`/`ui_panel_2.js`）：roundsDetail 新增 `pBaseDeal`（本回合确定性基准伤害快照，供 UI 预估）；怪物血条在「当前回合预估可击杀」时红闪 + 「☠ 可斩」标记，顶部显示基准伤害区间；暴击回合按倍率反除还原基准。
- **P0-2 绝境反制（以杀止杀）**（`combat.js`/`ui_panel_2.js`/`css`）：玩家血量 <25% 进入「绝境」：击杀怪物时回复 10% 最大气血 + 气势 +2；战斗层挂 `fb-desperate-on` 红雾边框角标，击杀回血/气势以浮字播报。
- **P0-3 西行劫难词条（Hades Heat 式自选进阶）**（`data_config.js`/`combat.js`/`game_combat_1.js`/`game_combat_2.js`/`main.js`/`ui_panel_2.js`）：通关/高难解锁后开局自选 1–3 条负面词条——妖气弥漫（敌攻+）/六根不净（玩家战意/经力获取衰减）/干粮短缺（战后回血削减）/心魔缠身（开局带心魔）/恶缘当道（善恶反噬），每条叠加「碎金酬」金价倍率，高难换高回报。
- **P1-1 精英/Boss 随从（前置肉盾）**（`combat.js`/`game_combat_1.js`/`enemies.js`/`ui_panel_2.js`）：玩家伤害先扣随从血条（`minion.hpPct` 按本体血量比例），随从存活期间本体不受伤害，破胆后溢出伤害直打本体；金角银角（精细鬼/伶俐虫 30%）、青牛精（看炉小童 35%）等标志性怪配随从；UI 随从独立血条显示。
- **P2-1 战意/经力溢出正反馈（满而不溢）**（`battle_resource.js`/`combat.js`/`game_core_3.js`/`ui_panel_2.js`/`css`）：战意满后溢出攒「昂扬」层（≤3），绝招时每层 +15% 伤害；经力满后溢出攒「化雨」层（≤3），诵经时每层回复 6% 最大气血；资源条金红脉动角标（昂扬×N/化雨×N）。
- **P2-2 释经/弃印（StS 删卡式精简）**（`game_rest.js`/`ui_panel_2.js`/`ui_misc_4.js`/`jieseals.js`/`main.js`）：土地庙新增「放下」操作——释经（残片换碎金 渡 8/16、逆 12/24；冗余片触发「经尘回向」定向补未完成全本）、弃印（按品阶换碎金 白6/蓝12/金20/红30，`SEAL_TIER_GOLD` 新增；非主道印触发「道印回向」补主道同阶印），缓解格子堆满无操作空间、收束构筑。
- **阻断 bug 修复**（`ui_panel_2.js`）：P0-2 绝境角标改动时在战斗模板行尾误加反引号，导致整个文件模板字符串提前闭合、浏览器解析即崩；已移除。
- **门禁**：新增 `scripts/_smoke_sutra_drop.js` **15/0**（渡/逆释经定价、冗余回向、弃印定价、道印回向、非法输入）；全量 smoke **26 组全绿**（含 `_smoke_monster_behavior` 29/0、`_smoke_combat_shipo` 19/0、`_smoke_sutra_route` 15/0）+ `bus_decoupling_test` + `validate_audio` 13/0。
- **版本戳**：style.css 441→442、combat.js 396→397、enemies.js 376→377、jieseals.js 379→380、battle_resource.js 374→375、data_config.js 376→377、game_combat_1.js 426→427、game_combat_2.js 424→425、game_core_3.js 433→434、game_rest.js 423→424、ui_misc_4.js 429→430、ui_panel_2.js 434→435、main.js 438→439。

### v=442（2026-09-05）V9.x 怪物行为引擎 · 动作扩展与全 Boss 专属脚本
**更新内容**：怪物行为引擎从「六动作」定型（新增 multi 多段连击 / heal 妖法回春），全部 10 个关隘 Boss 与 5 个标志性精英获得专属意图脚本，并修复行为参数读取链路与一处阻断 bug。
- **动作类型扩展**（`combat.js` `enemyAttack`）：`multi`=疾风连击（hits 段×hitMult 逐段独立闪避，总伤≈hits×hitMult×普攻，段伤轻不削气势）；`heal`=妖法回春（本回合不出手，回复最大血量 pct，制造「集火打断」压力）；roundsDetail 透传 `mHealAmt`/`mMultiHits`/`mMultiLanded`。
- **专属脚本注入**（`enemies.js`）：BOSS_TABLE 10/10 全配 `behavior`——刘洪（识破教学节奏）/黄风大圣（三昧神风）/五行归墟与大圣残躯（白骨三戏轮转）/青牛精（金刚琢套兵器·guard 蓄势）/太上老君（丹炉真火·buff 叠攻）/牛魔王（力沉重击）/狮驼岭三魔（连击轮转）/凌云渡金蝉（heal 脱壳重生）/终局 81 难（六动作全量轮转+回春集火）；ELITE_TABLE 增配 5 个标志性精英（白骨精三戏/金角银角葫芦吸摄/金兜洞青牛预习/蝎子精高频蛰刺/铁扇公主扇风）。
- **行为参数读取链路修复**（`combat.js`）：`guardPct`/`buffAtkPct`/`healPct` 此前写于 behavior 内联但从怪物顶层读取导致从未生效，改为 `act.pct > behavior.* > m.* > 默认` 四级回退，默认派生的参数（mob 0.25/elite 0.35/boss 0.40）同步生效。
- **阻断 bug 修复**（`combat.js`）：`playerAttack` 返回值误加 `multi/hits/hitCount`（引用未定义 `multiHits`），任何战斗开场即 ReferenceError；已移除，multi 字段仅在 `enemyAttack` 返回。
- **门禁**：`_smoke_monster_behavior.js` 扩充 4 节（multi 段数/闪避、heal 回复与 healPct、专属脚本注入+全 Boss 防漏配、behavior 参数生效）→ **26/0**；`_smoke_combat_shipo` 19/0、`_smoke_build_match_fight` 6/6、`_verify_battle_v853` CHECK 全绿。
- **版本戳**：style.css 440→441（上批）、combat.js 395→396、enemies.js 376→377。

### v=440（2026-09-05）方案X1 · 持诵位（诵经随经变）
**更新内容**：引入「持诵位」——经匣内可从已合成全本中任择一部持诵，诵经技能随持诵经 `chantSkill` 变更（念什么经，使什么法）；未设持诵回退本命/戾骨献祭。
- **数据层**（`data_sutra.js`）：25 本经（渡16+逆9）全配 `chantSkill`（name/cd/mult/kind/desc）；kind 六种：`zen-heal`（回血）/`war-buff`（激昂暴击）/`glut-ton`（吸血）/`ward-mantra`（护盾+穿刺真伤）/`veil-mantra`（必中真伤）/`break-mantra`（破甲真伤·新原语）；新增 API `chantSkillOf`/`chantSutraOf`/`canChantSutra`。
- **战斗层**（`combat.js`）：`activeSkill('chant')` 三态优先级——①持诵经 chantSkill（渡经吃渡侧拼篇、逆经吃逆侧拼篇）→ ②未持诵且已合成破戒录/逆天录 → 戾骨献祭（CD5 场限1）→ ③本命诵经；补 `break-mantra` 机制（法伤+30%破甲真伤，`armorBreak` 打标）。顺带修复戾骨献祭旧判定读 `sutraBox/niFullSutras` 死存储的问题，改为读现代 `s.niSutras`。
- **状态/读写**（`game_event_1.js` 状态初始化 `chantSutra:null`；`game_sutra.js` 新增 `setChantSutra`/`clearChantSutra`，含已合成校验）。
- **UI 层**：经匣（`ui_misc_4.js`）顶部持诵位横幅（当前经+诵经技能+取下），已合成卡片「设为持诵」/持诵中「取下」按钮，卡片展示诵经技能行；战斗三键（`ui_panel_1.js`）诵经按钮显示持诵技能名并挂 `chant-sutra` 金光高亮；style.css 补 `sx-chant-*`/`active-btn.chant-sutra` 样式；`main.js` 接线 `set-chant-sutra`/`clear-chant-sutra` 事件（操作后重开经匣刷新）。
- **门禁**：新增 `scripts/_smoke_chant_sutra.js` **31/0**（25经chantSkill齐全/API三件套/三态优先级/break-mantra进applyActiveIntervention/set-clear校验）；全量 smoke 24 组 + regression 153/0 全绿。
- **版本戳**：style.css 439→440、data_sutra.js 375→376、combat.js 393→394、game_sutra.js 423→424、game_event_1.js 435→436、ui_misc_4.js 428→429、ui_panel_1.js 432→433、main.js 436→437。

### v=439（2026-09-05）战斗节奏与手感 P0/P1/P2 三批落地（竞品借鉴）
**更新内容**：按《逆道西行_战斗节奏与手感_竞品借鉴分析_2026-09-05.md》落地全部三批优化
- **P0 表现层打击重量**：
  - 命中停帧分级：`_emitBattleFx` 按 `_kind` 发射 `hitstop`（crit 80ms / break 60ms / heavy 65ms / hit 50ms），`ui_misc_3._fxHitStop` 分级映射 + `void offsetWidth` 重放；style.css 补 `hit-stop-heavy` 关键帧。
  - 受击白闪：`ui_misc_3._fxHitFlash` 目标立绘挂 `hit-shake + hit-flash`（380ms 恢复）。
  - 伤害数字分级：`_kind` 优先级 `crit > shipo > burst > heavy > hit`，`_fxSpawnDamage` 动态 `damage-{kind}` 类；style.css 补 `damage-shipo`（金）/`damage-burst`（红）。
- **P1 决策与仪式感**：
  - 蓄力重击提前一轮预告：`combat.js` roundsDetail 新增 `telegraphSoon`；`ui_panel_2` 意图区「🌀 妖气凝聚」次级预警（紫），与 telegraph 互斥。
  - 战斗小结：胜利结算浮层 `wr-summary` 五维（回合/最高一击/识破/爆发/气势峰值），纯聚合零新状态；style.css 补 `.wr-summary` 样式。
  - Build 成型宣告：`main.js` `PHASE_INTRO` 经 `s.flags.buildAnnounced` 守卫 + `auditBuild(s).formed` 触发 `battle-fx build-formed`；`ui_misc_3._fxBuildFormed` 横幅「道途圆满 · 诸法归一」；style.css 补 `.fb-build-banner*` 与 climax 家族样式（`.fb-climax-*`/`.slow-motion` 此前 JS 有引用但 CSS 缺失）。
- **P2 节奏重心**：小怪目标回合上限 6→5（`Math.min(5,...)`，教学战跳过节奏校准）；精英战 routine 操作点每 5 回合→每 4 回合（`_routineEvery = elite ? 4 : 5`）。
- **门禁**：`_smoke_combat_shipo.js` 新增 telegraphSoon 4 例 → **19/0**；22 smoke 全绿；regression 153/0、platform 41/0、combo 43/0、telemetry 16/0、hidden_jobs 4/0、progress_floor 11/0、seal_pity 10/0、validate_audio 13/0。
- **修复**：`bus_decoupling_test.js` 装载口径——`_refactor_split` 后总线事件落 `js/game/*.js`，测试仅扫 `game.js` 致 `battle-fx` 断言失败；补扫子目录后全过。
- **版本戳**：style.css 438→439、combat.js 392→393、game_combat_1.js 424→425、ui_misc_3.js 428→429、ui_panel_2.js 432→433、main.js 435→436。

### v=2（2026-09-04）V8.55 套装共鸣实算核实 + 音频 M14 自检门禁
**更新内容**：两处"潜在死数据"疑点核实为实算，补两套验证门禁，并修复一处真机静默缺口
- **套装共鸣（`SET_RESONANCE` / `applySetResonance`）核实为实算**：
  - 真实机制按**同 set 身体四槽(weapon/armor/head/boots) ≥2 件(tier2)/≥3 件(tier3)** 触发（玄武减伤/盘缠经济/御兽随宠物缩放/英雄专属套…），在 `computeStats` 内 end-to-end 生效。
  - `setTier` 仅作掉落/合成稀有度阶（1=基座、≥2=成品红装），**并非**共振激活阈值。
  - 新增门禁 `scripts/_smoke_set_resonance.js` **16/0**。
- **音频 M14 自检门禁 + craft 缺口修复**：
  - 新增 `scripts/validate_audio.js` **13/0**：运行时桥接包装/ADAPTIVE_SFX 开关/headless 回退不抛/诊断守卫 + 静态 1:1（游戏侧每个 `NDX.sfx(name)` 调用名都在引擎 `G(name)` 注册）。
  - **修复真实缺口**：`game.js:5288/5305` 调用 `sfx('craft')`（锻造/合成成功）但引擎未注册 → 真机 ADAPTIVE_SFX=true 下静默无音；已补 `G('craft', …)` 合成基元（`ndx_audio_engine.js` `?v=2`）。

### v=411（2026-09-04）V8.54 英雄专属武器/护甲自动进阶 + 音频 M14 桥接收口
**更新内容**：补齐 V8.53（事件装备分池）之后的两项收口；版本标签与 V8.53 拆分
- **装备进阶**（`js/equipment.js` 命中 `?v=385`）：
  - 把 V8.52 的 `NDX.upgradeHeroTreasure` 泛化为 `NDX.upgradeHeroGear(s, slot)`，`slot ∈ treasure|weapon|armor` 三槽统一随章节自动进阶。
  - 匹配口径单一真源 `NDX.HERO_SET_NAME`：法宝按 `owner`、武器/护甲按 `set` 匹配；幂等、不降级、全量注册表扫描（`EQUIP_POOL+BOSS_REWARDS+CRAFT_POOL`）与法宝逻辑一致。
  - 五英雄武器/护甲 ch2/3/4 从「仅 GROWTH_RECIPES 手工合成死数据」变为随章节自动获得。
  - 老入口 `upgradeHeroTreasure` 委托 `upgradeHeroGear('treasure')` 保兼容。
  - `js/game.js` `?v=411` 的 `enterNode` 每节点对三槽统一收敛。
- **音频 M14 桥接收口**（`js/audio/ndx_audio_bridge.js` `?v=2`）：
  - 新增 `ADAPTIVE_SFX`（默认 true）+ 包裹 `NDX.sfx(name)`，自适应开且引擎就绪时路由 `A.playEvent('event:/SFX/Game/<name>')`，否则回退原 `sound.js`；暴露 `setSfxAdaptive/isSfxAdaptive`。
  - 23 个游戏同名合成 SFX 从「注册但未发声」转为真正路由。
- **门禁**：新增 `scripts/_smoke_hero_gear_upgrade.js` **26/0**；老法宝门禁 `_smoke_treasure_upgrade.js` **21/0**；回归 `regression.js` **149/0**；`node --check` 全过。

### 历史补录（已提交、原版本记录未备注的横屏/Guide/素材迭代）
> 以下条目此前已随 git 提交落地但未同步本文件，现补录归档：
- **观音指引/新手教学独立覆盖层**（`css/style.css` v329→v330）：立绘图卡+气泡覆盖，占用左侧窄槽（x26…154，宽128/高140，长文内滚），不挡战斗 UI。终位落「加载帧后治理」。
- **门票地图黑框根修**：`mapGeom` 用 `mapArea` 实宽 + 暗角/锁区压暗减弱，短横屏路径不再带黑框。
- **真横屏弹窗塌陷根修**：关闭钮选择器由 `[data-action*="close"]` 补 `:not(.scene-overlay)` 排除遮罩底，避免覆盖 `position:fixed`。
- **强制横屏终局层**：手机横版唯一形态（`v=326` 起），统一 `.ndx-short-landscape` 门控。
- **素材 WebP 化**：17 地区背景 + 英雄立绘 png→webp 重压 + `__pickWebp`/fallback 兜底（降低加载体积 30-60%）。

### v=386/408（2026-09-04）V8.53 事件装备分池+加权+保底
**更新内容**：解决顶级装备事件被大池稀释、六道选项多不提高概率的问题；四项全部实施
- **用户拍板口径**：1+2+3+4 全部实施（装备加权/六道加权/保底/分池）。
- **events.js**：
  - 新增 `NDX._isGearEvent(ev)`：判断事件是否带 gear（任一选项有 gear 字段）
  - 新增 `NDX.sutraGearPool(s, act)`：当前地区装备事件子池
  - 新增 `NDX.sutraNormalPool(s, act)`：当前地区普通事件子池（经文/善恶抉择）
  - 新增 `NDX.weightedSutraPick(pool, s)`：加权随机选事件——①装备顶级(地区≥12)×1.5、次级×1.2；②六道属性对应side加权（善系渡/缘/隐总和越高ferry事件权重越高，恶系战/夺/逆同理，每点+10%上限+100%）
  - 修复 SUTRA_EVENTS 稀疏数组 bug（V8.44 事件前多余逗号导致 1 个 undefined 元素，153→152）
- **game.js**（L1276-1310 触发逻辑重写）：
  - ④分池判定：非保底时 30% 概率进装备子池、70% 进普通子池（原均匀随机被大池稀释）
  - ③保底机制：维护 `s._gearEventMiss` 连续未出装备事件计数；连续 3 个问号未出装备事件，下一个 100% 触发且强制装备子池；出了装备事件 reset，未触发/出普通事件则++
  - ①+②加权：子池内用 `weightedSutraPick` 加权随机（原 `pool[Math.floor(Math.random()*pool.length)]` 均匀随机）
  - 触发概率：保底时 100%，否则保持 50%
- **验证**：node --check 0 失败；临时脚本 16/0（分池/加权/保底/六道属性加权/稀疏数组修复全通过）；regression **149/0**；浏览器实机 0 JS 错误 + 4 新函数注册 + 稀疏数组修复确认（152/152）。
- **效果**：地区12 单个顶级装备事件实际触发概率从 1.9% 提升至约 4.5%（30%分池×1.5加权×50%触发），保底确保脸黑玩家也能拿到；六道属性高的玩家对应路线事件概率显著提高（善系8点时ferry占比78.5% vs 恶系高时53.6%）。
- **提交范围**：events.js/game.js/index.html/版本更新记录（v=386/408）。

### v=382/385（2026-09-04）V8.52 渡/逆顶级事件装备 + 次级事件补发
**更新内容**：事件专属装备路线扩充（每条路线次级→顶级升级链）+ 修复 5 件次级无发放死数据
- **用户拍板口径**：接入土地庙装备组合面板（V8.50 已落地）+ 补充更多事件装备让每条路线选择更丰富；事件顶级需有组合件/升级件升级链；boss/精英掉落 + 土地庙极低概率补刷（V8.50 已落地）；冒险日记式超多装备组合感。
- **equipment.js**：
  - `EVENT_GEAR` 尾部新增 10 件渡/逆**顶级**事件装备（每路线 5 槽位，均 eventOnly）：
    - 渡线（法伤/渡化/回血向，数值略低于通用顶级）：`ev_w_jiedu` 净渡锡杖（愿伤124+吸血2%）、`ev_a_puti` 菩提金身（血660/盾12%）、`ev_h_rulaizang` 如来藏冠（愿伤88+暴伤15%）、`ev_b_jieyin` 接引莲台靴（闪17%速3+血60）、`ev_t_bafu` 八宝功德斛（反8%吸5%）
    - 逆线（物攻/破甲/掠夺向）：`ev_w_kuanglong` 狂龙戟（攻152+破甲30+无视甲6%）、`ev_a_mojiang` 魔将玄甲（攻30+血640/盾11%）、`ev_h_zhanshen` 战神冠（体攻55+愿伤55）、`ev_b_yasha` 夜叉逐风靴（闪18%速4）、`ev_t_panyu` 盘狱炼魂铃（反9%吸4%）
  - `RECIPES` 尾部新增 10 条 eventCombo **升级链配方**：次级事件装备 + 组合件/升级件 → 对应路线顶级（如 柳杖·净+灵蕴珠→净渡锡杖、逆鳞刀+妖魂核→狂龙戟）。与游历散宝「初等→锻→顶」链同构，事件+合成双轨获取。
- **events.js**：`SUTRA_EVENTS` 尾部新增 15 个事件——
  - 5 个**次级补发**（修复并行新增次级装备无事件发放的死数据）：宝相冠（渡7）、莲步履（渡4）、甘露宝囊（渡8）、修罗冠（逆6）、踏火靴（逆6）
  - 10 个**顶级发放**（地区 12-16）：净渡锡杖12、菩提金身13、如来藏冠15、接引莲台靴14、八宝功德斛12、狂龙戟13、魔将玄甲12、战神冠15、夜叉逐风靴14、盘狱炼魂铃16
  - 全部经事件选项 gear 授予（与经文/善恶竞争，不可稳定获取），逆线需本局选过逆。
- **验证**：node --check 0 失败；临时脚本 89/0（30 件事件装备注册/10 配方/15 事件发放/地区分布/合成链路模拟）；regression **149/0**；浏览器实机 0 JS 错误 + 新装备注入确认。
- **提交范围**：equipment.js/events.js/index.html/版本更新记录（v=382/385）。

### v=383/380（2026-09-03）V8.44 事件专属装备体系
**更新内容**：每装备槽位独立特殊装备（顶级/次级/英雄专属三级梯度）+ 多样化合成（参照冒险日记）
- **用户拍板口径**：① 每装备栏有独立特殊装备，优于英雄专属顶级，但单件不及 4 件英雄专属组合套件；② 次级属性高于英雄专属、但不稳定拿到（逼迫玩家多走事件）；③ 多样化合成（饰品+饰品、武器+防具双件套、单件事件升级）。
- **equipment.js**：
  - 新增 10 件事件专属装备（`SUTRA_EVENT_GEAR` 追加，5 槽 × 次级/顶级），全部 `eventOnly`（不入随机掉落/商店，仅事件发放）：
    - 武器（物理极高+破甲）：`ev_w_langya` 啸天狼牙（次级 atk118/fixAtk15）、`ev_w_xingtian` 开天斧·刑天（顶级 atk165/fixAtk35/armorPen0.12）
    - 甲胄（自带护盾）：`ev_a_wudang` 无当袈裟（次级 hp560/dr16%/shieldPct8%）、`ev_a_ruyi` 金缕玉衣·如来（顶级 hp760/dr22%/shieldPct15%/mdef6%）
    - 头冠（法伤暴击）：`ev_h_pilu` 毗卢遮那冠（次级 matk66/crit5%）、`ev_h_wufo` 五佛冠·真（顶级 matk92/crit8%/criMult0.2）
    - 战靴（闪避速度）：`ev_b_dengyun` 登云履（次级 eva13%/spd2）、`ev_b_tayun` 踏云追风靴（顶级 eva19%/spd4）
    - 法宝（反伤吸血）：`ev_t_hunyuan` 混元一气袋（次级 reflect6%）、`ev_t_shanhe` 山河社稷图·残（顶级 reflect10%/lifesteal4%）
  - 新增 3 件多样化合成产物（CRAFT_POOL）：`cf_wa_shashen` 弑神·不坏·双绝（武器+甲胄→双件套武器）、`cf_t_hunhe` 混元社稷图（法宝+法宝→高级饰品）、`cf_a_wudangjin` 无当金身（甲胄+材料→升级）
  - 新增 3 条配方（RECIPES）：双件套 comps、高级饰品 comps、甲胄升级 growth
  - `_equipScore` 评分纳入 armorPen/shieldPct/reflect/criMult/lifesteal/spd
- **combat.js**（工作区，含并行改动）：`computeStats` 收集 `equipArmorPen`，返回值加 `armorPen`；`simulateSingle` 玩家物理攻击基础穿透怪物减伤（`effMDr2 - armorPen`，与齐天命痕可叠加）。
- **game.js**（工作区，含并行改动）：calcCombat player 构造透传 `armorPen`。
- **ui.js**（工作区，含并行改动）：两处背包 stat 补充 crit/fixAtk/armorPen/shieldPct/reflect/spd/lifesteal 展示。
- **events.js**：新增 10 个事件专属装备发放事件（次级地区 4-8、顶级 12-16），side 渡/逆对应，gear 发放与经文竞争。
- **验证**：node --check 全量 0 失败；regression 149/0；platform_test 41/0；bundle 重建 58 文件 1737KB；浏览器实机 0 JS 错误、英雄选择界面正常。
- **提交范围**：equipment.js/events.js/index.html/bundle.js 已提交；combat/game/ui 混有并行改动留工作区（armorPen 机制随并行提交带上）。

### v=375（2026-09-02）
**更新内容**：删除五行系统（用户拍板）+ 装备组合体系前置清理
- **用户拍板**：五行系统无存在必要 →「直接删除五行系统」。按宪法「用户明确否定的概念必须从代码、文档、计划和命名中删除」执行全量删除。
- `data_config.js`：删 `NDX.WUXING`（COUNTER/ICON/COLOR/COUNTER_BONUS/calcMulti/weaknessOf）、`NDX.HERO_WUXING`（5 英雄本命）、`NDX.REGION_WUXING`（17 地区），各留 V8.43 删除注释。
- `equipment.js`：删 5 件五行 trinket（gengjin_ling/shenmu_pei/bishui_zhu/bihuo_zhu/huangtu_fu）；删 `NDX.TRINKET_SLOT`/`NDX.trinketSlotCap`/`EQUIP_SLOT_LABEL` trinket 键；`activeEquipsFor` 移除 trinket 选择（饰品槽体系整体移除）。
- `icon_map.js`：删五行条目（基础/升级版）+ `bishui_zhu` 孤立条目 + generic_trinket.png 映射。
- `combat.js`：删玩家攻击属性读取/输出、怪物克玩家结算、玩家克怪物结算（wuxingState='counter'/'countered' 乘倍率）、返回值 wuxingState/mWuxingState 字段。
- `game.js`：删怪物 wuxing 赋值、玩家 element 透传。
- `enemies.js`：删 rollMob 五行分配。
- `ui.js`：删饰品槽整块渲染+计数、玩家五行图标、怪物五行图标+弱点、飘字 wuxing class；bagSlotPickerHtml/bagDetailModal/_slotIco 中 trinket 键。
- `main.js`：删 battle-fx wuxingState 透传。
- `attr_calc.js`：删 'wuxing' 来源 label。
- `data_equip_core.js`：rollEquips loosePool/avail3 中 `e.slot === 'trinket'` 条件移除（五行饰品已删，trinket 无装备）。
- `data.js`：五行历史注释块更新为 V8.43 删除说明。
- `scripts/regression.js`：删 3 条 WUXING 克制断言 + 测试玩家对象 element 字段。
- `index.html`：10 个改动 js 版本号 →375；minigame/bundle.js 重建（56 文件，1673KB）。
- 验收：全量 `node --check` 0 失败；regression.js 149/0（原 152/0 减 3 条五行断言）；smoke_hidden_jobs.js 4/0；platform_test.js 41/0；浏览器实机 v=375 0 JS 错误、装备面板生效格 [装4·宠2·宝2·印3] 无饰品槽、页面文本零五行残留。
- **后续**：原任务「整理全部装备：所有散件需有组合件、四件触发效果、土地庙装备组合、包裹生效、隐藏专职前提」继续——五行删除后真散件已清零（仅事件装备 21 件为剧情专属），需在土地庙落地装备组合面板。

### v=371（2026-09-02）
**更新内容**：装备体系整理（二）——取消散件/无用套装，六道四件套升级链同构铺开
- **用户拍板口径**：① 取消散件、取消无用套装；② 以贪狼模式为模板，为破军/玄武/影遁/逆命四套同构建立"四槽三阶四件套→3组件→套装独立隐藏职三转"升级链；③ 六道专职套（影遁/逆命）一起做升级链；④ 黑风/狮驼/凌云 保留但补共鸣。
- `equipment.js`：
  - **删除 7 件孤儿散件**（langya 狼牙棒/tiegou 铁钩/shutong 熟铜锏/caoxie 芒鞋/douli 苇笠/fuguan 鎏金冠/xuanxue 玄铁战靴），只被 icon_map 引用、无配方无共鸣无掉落渠道。
  - **删除影遁/逆命旧三件套体系**（yin_w/a/t_base + T2 + T3 共 12 件 + 6 条合成配方）：已被新四件套升级链取代，旧 id 全库零引用。
  - **EQUIP_POOL 新增四套四槽三阶四件套各 12 件**（共 48 件）：破军(pw·战·攻击向) / 玄武(xw·渡·防御向) / 影遁(yd·隐·闪避向) / 逆命(nm·逆·全加向)，chapter 1/2/3 逐章解锁、setTier:1 可掉落。
  - **CRAFT_POOL 新增 12 个套装组件**（slot:'component'，每套 3 个，compTier 1/2/3），desc 写明激活转职名与属性。
  - **RECIPES 新增 12 条组件合成配方**（comps 链，无材料消耗）：四件T1→组件1；组件1+四件T2→组件2；组件2+四件T3→组件3。
  - **SET_JOBS 新增 3 套**（破军/影遁/逆命）+ 启用玄武预留：五套三转齐全，6 道覆盖（贪狼=缘、破军=战、玄武=渡、影遁=隐、逆命=逆）。
  - **SET_RESONANCE 补齐 5 套**：影遁/逆命（六道专职）+ 黑风/狮驼/凌云（章节套保留补共鸣）。
  - `_equipKind`/`slotCapForKind` 增加 component 分类（组件不占生效格）。
  - SUTRA_EVENT_GEAR 打 `eventOnly` 标记（事件装备只由剧情发放，杜绝混入随机掉落/商店）。
- `data_equip_core.js`：`rollEquips` loosePool/avail3 兜底排除 eventOnly 与无 set 散件（散件彻底取消；五行饰品 trinket 保留作稀有掉落）；SET_DAO 删幽行空壳、逆命补'逆'；SET_SYS 补影遁/逆命。
- `attr_calc.js`：7.5 节组件生效新增 crit 键结算（破军/逆命三转含暴击）。
- `icon_map.js`：删除 7 条旧散件条目 + 新增四套四件套/组件 60 条映射；修复 `getEquipIcon` 中文标识符变量（专属Icon）导致的运行时错误。
- `ui.js`：完整背包弹窗组件专用展示——中文槽标签「灵性」+「⚡ 灵性生效」徽标（显示激活的套装隐藏职），替代无意义的「置为生效」。
- `index.html`：版本号 370→371；minigame/bundle.js 重建（56 文件）。
- 验收：数据层加载测试（四套 48 件全注册、12 组件/12 配方链全完整、setJobByComp 全命中、EQUIP_POOL 无 set 散件=0）；浏览器实机（页面 0 JS 错误、升级链闭环 comp1→comp3 属性逐级生效、crit 键结算、背包组件渲染）；node --check 0 失败；regression.js 152/0；smoke_hidden_jobs.js 4/0；platform_test.js 41/0。

### v=370（2026-09-01）
**更新内容**：装备体系整理——贪狼套装升级链 + 套装独立通用隐藏职（SET_JOBS）
- `equipment.js`：
  - `EQUIP_POOL` 新增贪狼四件套 12 件（T1 初刃/初甲/初盔/初靴 chapter1、T2 中刃/中甲/中盔/中靴 chapter2、T3 天狼刃/天狼甲/天狼盔/天狼靴 chapter3），数值低于封神/轮回 T3 未越档；全 set:'贪狼' setTier:1。
  - `CRAFT_POOL` 新增 3 个套装组件（slot:'component'，setTier:2 不参与掉落）：tl_comp1 贪狼·聚灵 / tl_comp2 贪狼·凝魂 / tl_comp3 贪狼·天狼。
  - `RECIPES` 新增 3 条组件合成配方（comps 链，无材料消耗）：四件T1→组件1；组件1+四件T2→组件2；组件2+四件T3→组件3。符合用户拍板"四件初级套→组件1→一转→四件中级套→组件2→二转→4升级组件→组件3→三转"。
  - 新增 `NDX.SET_JOBS` 套装独立通用隐藏职表（贪狼三转已定义，玄武预留）：组件经包裹生效、不占装备格，任何英雄走该套均可得，不与英雄隐藏职重合（如唐僧走玄武套补防御不影响法伤）。提供 `NDX.setJobByComp` / `NDX.setJobBonusFor`。
  - `applySetResonance` 共鸣件数统计限定身体四槽（weapon/armor/head/boots），排除组件与历史法宝/宠物挂的 set 标签，避免持有 2 件历史贪狼法宝误触贪狼共鸣。
- `attr_calc.js`：`applyPostProcessing` 新增第 7.5 节「套装独立通用隐藏职生效」——遍历 s.equips 中 component:true 项，按最高转职档叠加固定值（攻/血/减伤/闪避/反伤），jobs 写回 `s._setJobs`。
- `data_equip_core.js`：修复 `rollEquips` basePool 分支 `out.push(e)` 引用未定义变量（初始基线即有的破坏代码，basePool 非空时必崩）→ 改为 `basePool.splice(idx,1)[0]`。
- `index.html`：版本号 →370（与并行会话同步）；minigame/bundle.js 重建（56 文件）。
- 验收：贪狼升级链专项 27 项全过；全量 `node --check` 0 失败；regression.js 152/0；smoke_hidden_jobs.js 4/0；platform_test.js 41/0。

### v=368（2026-09-01）
**更新内容**：P2 专属剧情·叙事高光区补全（祭赛国 / 狮驼岭 / 比丘国）
- `events.js`：三个叙事高光区各新增 3 段 `region` 限定专属剧情（共 9 事件 36 选项），配合原 r12/r13/r14 各 1 段，中后段不再靠通用随机池兜底。
- 祭赛国(12)·金光寺舍利疑案：宝塔失光 / 龙宫夜宴 / 九头低语——舍利失窃、万圣公主、九头虫执念。
- 狮驼岭(13)·四百里尸山：白骨袈裟 / 大鹏金羽 / 青狮回响——前代取经人白骨、大鹏"你不是第一个"、白骨铺成的路。
- 比丘国(14)·丹炉与名册：丹炉小儿 / 鹿角名册 / 国丈长明灯——以孩童心肝炼药的暴政、一千一百一十一个名字、长生油。
- 六道选项新增分布：渡7 缘7 隐6 战3 夺8 逆5（善20 / 恶16），刻意多给缘/夺短板；全量六道 渡356 缘132 隐133 战162 夺111 逆260，善621 / 恶533。
- `index.html`：?v=367→368；minigame/bundle.js 重建（56 文件）。
- 验收：全量 `node --check` 0 失败；regression.js 152/0；platform_test.js 41/0。

### v=366（2026-09-01）
**更新内容**：P2 落地——regionToTier() 装备档位映射统一 + 六道选项微调
- `data_compound.js`：正式落地 `NDX.regionToTier(region)`（17地区→4档装备映射：地区1-4→1 / 5-9→2 / 10-13→3 / 14-17→4），`regionToEquipChapter` 保留为兼容别名。
- `data_equip_core.js` / `data_equipment.js`：4 处装备章节消费点（掉落池 cap、章节套件解锁、cave 红材）统一走 `regionToTier()`，消除凌云套(chapter:4)从地区4(难14)提前约47难入池的标尺错位。
- `events.js`：修正 11 处六道标签错位（文本【缘】+ 双防 effect + 善，但 fate 误标 渡/隐 → 改为 缘），仅改标签归属不动 effect/善恶/剧情；缘道选项 114→125。
- 六道选项分布（events+trials81+主线）：渡349 / 缘125 / 隐127 / 战159 / 夺103 / 逆255；善恶比 601:517 不变，后续内容持续微调逼近均衡。
- `data_sutra_shard.js`：从归档恢复至 demo/js/（并行会话 index.html 保留引用，消除 404；文件仍为预留的经文碎片系统载体）。
- `index.html`：版本号 ?v=365→366；minigame/bundle.js 重建（56 文件）。
- 验收：全量 `node --check` 0 失败；regression.js 152/0；platform_test.js 41/0（含 bundle 漂移防护）。

### v=364（2026-09-01）
**更新内容**：P3 工程清理——死数据/孤儿文件清理 + git 工作区基线
- `js/data_trials.js`：移除 15 条 TRIAL_LIB 旧段死数据（九章制残影 act:1/2/3、good/evil 旧键名，被 trials81.js 直接赋值覆盖永不生效），491→329 行，保留 HIDDEN_JOBS/HIDDEN_TRIAL_REQ/evalHiddenCond/isCycle1 等隐藏职数据。
- `js/data_sutra_shard.js`（41 行，经文碎片系统孤儿文件，SUTRA_SHARD/sutraTierOf/sutraTierGated 零外部引用）：归档至 D:\xiyou\原始素材\code_archive\，供后续复活；移除 `index.html` 加载引用。
- `index.html`：移除 data_sutra_shard.js 引用，版本号 ?v=363→364（60 处）。
- `minigame/bundle.js`：按 index.html 重建（55 文件，不再含经文碎片段）。
- git 工作区基线：demo 全量入库（代码/文档/资产/三端适配层），清除 8.9 归档索引残留（125 条），PNG 归档后删除 6 个原始 png。
- 验收：全量 `node --check` 0 失败；regression.js 152/0；platform_test.js 41/0（含 bundle 漂移防护）。

### v=357（2026-09-01）
**更新内容**：内容合规整改——宗教敏感表达脱敏（保留批判性立意，矛头对准「人间怪相」而非宗教本身）
- 职名「谤佛金蝉」→「弃经金蝉」（系统级改名，波及 achievements/attr_calc/combat/data_trials/events/game/hero_trials/trials81 及 game.js 逻辑 indexOf 同步）；难27「弃经·谤佛」→「弃经之问」；事件 d12「谤佛」→「破执」。
- 难1「金蝉遭贬」：如来不再作恶（账簿/判罚移交「上座执事」，佛判→法判）；保留金蝉子对「经书是枷锁」的质疑。
- 难56「小雷音假佛」：修病句「的人种袋」→「那人种袋」，弥勒→山门护法（id80 同步）；玄英洞「真佛收得太贵」→「香火供奉收得太贵」。
- 难77「无字碑」：灵山没有佛→灵山不见佛影；经即牢→有人把经当牢（hero_trials 悟空剧情副本同步）。
- 事件「破执」：佛像→泥塑金身/泥胎（推倒/画叉对象脱敏），「你敢谤佛吗」→「你敢掀这泥胎吗」；「谤佛杖」→「弃经杖」「如来若圆满，何惧一谤」→「若经圆满，何惧一弃」。
- 真实名号脱敏：难74「阿傩迦叶·索人事」→「传经索人事」（敌人「阿傩迦叶」→「传经吏」，含 enemies 数据/映射/地区名/章节列表）；朱紫国「佛母」→「神兽」。
- 保留（正面/中性）：真假美猴王如来辨妖、接引佛问话、弥勒渡口点化对话、data_trials 金蝉背景。
- 详表《逆道西行》黑暗西游81难详表.md 同步 9 处；`index.html` 版本号 v=357。
- 验收：全量 `node --check` 0 失败；regression 152/0、platform_test 41/0、smoke_hidden_jobs 4/0；CDP 浏览器 0 JS 错误 + 职名/文本生效；敏感词全项目残留 0。
### v=353（2026-09-01）
**更新内容**：音频整改——自制 8 条氛围音轨替换程序化合成 BGM
- 背景：原 `sound.js` 为纯 Web Audio 程序化合成（五声音阶单音循环 + 持续垫音），用户反馈「背景音频为噪音，引起玩家不适」。
- 自制 8 条音轨（AI 音频工具生成 → MediaKit 转码 MP3 128kbps 单声道 44.1kHz）：BGM×5（title/map/fight/boss/home 各 ~30s）+ 音效×3（sfx_click 木鱼点击 / sfx_zhuanjie 转职觉醒 / sfx_worship 诵经梵呗），放 `assets/sound/`，总计约 2.6MB。原始 WAV 留 `assets/sound/raw/`（自制凭证）。
- `js/sound.js`：BGM 改 `HTMLAudioElement` 播放文件（loop），音效 click/levelup 文件优先 + 新增 worship；Web Audio 合成保留为降级方案；音量联动（BGM=主音量×0.9×0.35）与静音恢复逻辑修正。
- `serve.ps1`：content-type map 补 `mp3='audio/mpeg'`。
- `index.html` 版本号 v=353。
- 验收：全量 `node --check` 0 失败；`regression.js` 152/0、`platform_test.js` 41/0、`smoke_hidden_jobs.js` 4/0；CDP 浏览器验证页面 0 JS 错误 + BGM/音效播放链路正常。详见 `docs/音频音轨说明_2026-09-01.md`。

### v=352（2026-09-01）
**更新内容**：六道选项纯善恶化（去奖励化）
- 用户最终拍板：六道选项不再附带任何属性/素材/法宝奖励，只加善恶与道途命数；善恶纯剧情点（道心/成就/心魔/剧情），不加属性；善恶分组维持 渡/缘/隐+善、战/夺/逆+恶。
- `js/trials81.js`：清除 72 处六道选项 effect 中的奖励键（hp/eva/atk/matk/mdef/dr/maxhpPct/material/treasure/yuan）+ 7 处选项级 reward（战道 atk、隐道 eva）；保留 alignGood/alignEvil/ally(收服随从×2)/d29Ni(机制flag)/fight 等机制键；label 微调 2 处（真葫芦到手→强夺紫金葫芦、得真扇→借得扇法真言）。
- 夺道 5 法宝选项（真葫芦/三昧真火/金刚琢/芭蕉扇/犀角灯）+ 渡/缘/逆的 3 法宝选项 reward 全部移除——法宝不再由六道选项直接获得，改由合成链/剧情节点承接。
- 详表重建（1000 行，选项 effect 同步清理）；`index.html` 版本号 v=352。
- 验收：全量 `node --check`、`regression.js` 152/0、`platform_test.js` 41/0、`smoke_hidden_jobs.js` 4/0、六道选项残留奖励=无 全过。

### v=351（2026-09-01）
**更新内容**：六道属性简化 + 劫印属性调整
- 六道选择回归最简：**不加任何属性，只加善恶**（选项仅保留叙事奖励），属性体系全部由劫印承担。
- 劫印六道属性对齐：战+攻 / **渡+血** / 缘+防 / **夺+反** / 隐+闪 / 逆+全加（反伤为主）。
- `js/jieseals.js`：SEAL_DAOTU 渡 matk→maxhp、夺 maxhp→reflect；SEAL_WORDS 渡道 8 词条→maxhp、夺道 8 词条→reflect（desc 同步）；逆道保持 reflect。
- `data_heroes.js` 死亡复盘文案「夺道气血劫」→「渡道气血劫」。
- 影响：渡劫印改血（唐僧法伤劫印来源减少，靠善念转法伤补）、夺劫印改反（八戒血劫印来源减少，靠护盾+善缘补）。
- 验收：全量 `node --check`、`regression.js` 152/0、`platform_test.js` 41/0、`smoke_hidden_jobs.js` 4/0 全过。

### v=349（2026-09-01）
**更新内容**：各英雄转职路线补齐（确保 2-3 条数值路线）
- 审计发现：八戒 2 转职全「攻血防吞食」（且都逆道）、小白龙 3 转职全「攻暴残血」——路线单一。
- 新增 2 个隐藏职：**八戒·天蓬·负岳**（难23，渡+善≥30，血+防+反伤 mReflect0.25）、**小白龙·白龙·御水**（难26，隐+隐≥3，血+防+闪避+首击免伤）。
- 八戒→逆道攻血防吞食 / 渡道血防反伤；小白龙→攻暴残血 / 血防闪避，五英雄均 ≥2 条路线。
- 难23 补「渡」选项；`HIDDEN_TRIAL_REQ` bajie+23、xiaobailong+26。
- 验收：全量 `node --check`、`regression.js` 152/0、`platform_test.js` 41/0、`smoke_hidden_jobs.js` 4/0 全过；详表重建（993 行）。

### v=348（2026-09-01）
**更新内容**：六道选项平衡（避免某道数量偏少）
- 重排后六道失衡（战49/渡66/逆46/隐23/夺12/缘21），用户要求平衡数量。
- `js/trials81.js` 给 34 个非 Boss 节点各加 1 选项：夺 +18（12→30）、隐 +7（23→30）、缘 +9（21→30），effect 用安全键，单难最多 4 选项。
- 平衡后：战49 / 渡66 / 逆46 / 隐30 / 夺30 / 缘30（渡为主线略多保留）。
- 全量 `node --check`、`regression.js` 141/0、`platform_test.js` 41/0、`smoke_hidden_jobs.js` 4/0、`_test_hidden_jobs.js` 49/0 全通过。
- 详表重建（含新选项）、映射表 §八、开发文档 §61 同步。

### v=346（2026-09-01）
**更新内容**：八十一难·原著地理重排（无返程 / 新增简版 / 同事件合并）
- 用户拍板「按原著《西游记》地理顺序重排」（路线 B）：`ACT_RANGES` 17 地区难号区间不动，重排区间内劫难内容，每区=其地名原著内容。
- `js/trials81.js`：TRIAL_LIB 81 难按 OLD_TO_NEW 映射搬移 + 新增 31 段简版 + 返程 30 段（原 57-80 + 并入的 24/34/36/37/40/53）移入 `NDX.RETURN_TRIALS` 隐藏保留（无返程进正传）；TRIAL_BOSS 重排补全 1-81；补 6 处 hidden（卷帘镇妖/金蝉了缘/天蓬复称/夺宝龙子/白衣渡客/六耳·残）；plot 标记（bis_shui_hua/jiushiyin/fuxinwang）。
- `js/hero_trials.js`：五英雄按新难号搬键；`js/data_trials.js`：HIDDEN_JOBS/HIDDEN_TRIAL_REQ 换新难号；`js/data_regions.js`：ACT_MAP_THEME act2-17 重做；`js/enemies.js`：BOSS_NAMES 17 条重写；`js/data_map.js`：FIXED_EVENT_TRIALS=[8,73]。
- 回归脚本难号断言同步：`regression.js` 141/0、`platform_test.js` 41/0、`smoke_hidden_jobs.js` 4/0、`_test_hidden_jobs.js` 49/0，运行时难号-地区一致性探针 11/0，全量 `node --check` 通过。
- 文档同步：映射表终版 `docs/81难地理重排_映射表_2026-09-01.md`、开发文档 §60、详表按代码重建。
- 已知债务：data_trials.js 15 条死数据未清理；龙太子归正传暂不可触发（难9 被斗战明王 hidden 占用）；RETURN_TRIALS 为后续返程素材。

### v=180（2026-08-28）
**更新内容**：
- 完成P2"收尾"的粒子对象池控性能功能
- 在ui.js中添加粒子对象池功能：_particlePool对象池（slash/projectile/climaxParticle）
- 添加get(type)方法：从对象池获取元素
- 添加put(type, el)方法：将元素放回对象池
- 添加clear()方法：清空对象池
- 设置maxPoolSize: 20（最大对象池大小）
- 所有JS文件语法检查通过

### v=179（2026-08-28）
**更新内容**：
- 完成P2"收尾"的特效密度开关功能
- 在ui.js中添加特效密度设置功能：effectDensity全局变量（high/medium/low/off）
- 添加shouldTriggerFx(level)检查函数，根据特效密度设置决定是否触发特效
- 在_fxClimax函数中添加了特效密度检查
- 所有JS文件语法检查通过

### v=178（2026-08-28）
**更新内容**：
- 完成P1"建立攻击→目标因果链"的所有内容
- 在style.css中添加弹道线的CSS样式：普通弹道线（金色）、暴击弹道线（红色，更粗）、重击弹道线（橙色，较粗）
- 在ui.js中添加弹道线的触发函数：_fxProjectile(kind)
- 在main.js中添加弹道线的触发调用：玩家攻击时触发弹道线效果
- 在ui.js的battle-fx事件处理中添加projectile类型的处理
- 所有JS文件语法检查通过

### v=177（2026-08-28）
**更新内容**：
- 完成P0"自动回放节点化（张弛）"的高潮层效果
- 在style.css中添加高潮层效果的CSS样式：慢镜（slow-motion）、全屏闪光层（fb-climax-flash）、大粒子弹射（fb-climax-particles）、震屏加重（climax-shake）、大字台词（fb-climax-text）
- 在ui.js中添加高潮层效果的触发函数：_fxClimax(type, text)
- 在main.js中添加高潮层效果的触发调用：暴击时触发（文字：暴击!）、破韧时触发（文字：业障破碎!）、狂暴时触发（文字：狂暴!）
- 在ui.js的battle-fx事件处理中添加climax类型的处理
- 所有JS文件语法检查通过

### v=176（2026-08-28）
**更新内容**：
- 检查P0"自动回放节点化（张弛）"的高潮层效果实现情况
- 确认已有高潮层效果：arenaSkillFlash（技能演出时的屏幕闪光）、fbClash（碰撞图标动画）、fbFloat（飘字效果）
- P0"自动回放节点化"大部分内容已实现，待增强：慢镜、全屏闪光层、大粒子弹射、震屏加重、大字台词
- 由于这是一个复杂的任务，需要逐步实施，待后续继续优化

### v=175（2026-08-28）
**更新内容**：
- 完成P1"提升参与感"的敌人头顶"下一步意图"显示
- 在ui.js中添加敌人头顶的"下一步意图"显示逻辑（基于telegraph、enraged等状态）
- 在style.css中添加fb-intent的CSS样式（脉冲动画、颜色区分）
- 意图类型包括：⚔攻击（红色）、💥蓄力重击（金色）、🔥狂暴（深红色）、🔥狂暴中（浅红色）
- 所有JS文件语法检查通过

### v=174（2026-08-28）
**更新内容**：
- 检查combat.js中的敌人AI逻辑，确认已有telegraph（识破窗口）概念
- 敌人行动逻辑包括：telegraph（蓄力重击）、enrage（狂暴）、普通攻击
- 可以基于现有telegraph概念来实现敌人头顶的"下一步意图"
- 由于这是一个复杂的功能，需要修改战斗逻辑和UI渲染，待后续实施

### v=173（2026-08-28）
**更新内容**：
- 检查P1"提升参与感"的敌人头顶"下一步意图"实现情况
- 确认combat.js中没有敌人下一步意图的实现，需要添加这个功能
- 敌人头顶的"下一步意图"应该包括：攻击（显示攻击力）、格挡（显示防御力）、狂暴（显示狂暴状态）
- 由于这是一个复杂的功能，需要修改战斗逻辑和UI渲染，待后续实施

### v=172（2026-08-28）
**更新内容**：
- 检查P1"提升参与感"的实现情况
- 确认已有操作点高亮QTE窗口：op-point-panel、op-countdown-ring、momentum-bar、op-treasures、op-skip-btn（ui.js:3225-3284）
- P1"提升参与感"操作点高亮QTE窗口已实现，待增强：敌人头顶的"下一步意图"

### v=171（2026-08-28）
**更新内容**：
- 检查P1"建立攻击→目标因果链"的实现情况
- 确认已有刃光效果：fb-slash-burst、fb-slash-arc、fb-slash-spark（style.css）
- 确认已有_fxSlash函数实现（ui.js:3014-3033），在敌方头像处创建刃光爆发效果
- P1"建立攻击→目标因果链"大部分内容已实现，待增强：从你方立绘到目标之间的弹道线

### v=170（2026-08-28）
**更新内容**：
- 检查P0"自动回放节点化（张弛）"的实现情况
- 确认已有战斗特效：狂暴、破韧、操作点、伤害飘字、震屏、刀光、击退、命中停顿、暴击台词、受击台词、紧箍特效
- P0"自动回放节点化"大部分内容已实现，待增强：慢镜、全屏闪光层、大粒子弹射、震屏加重、大字台词

### v=169（2026-08-28）
**更新内容**：
- 完成P0"让角色动起来"的所有内容
- 添加敌方击退knockback动画效果（style.css）
- 添加knockback触发逻辑（main.js）
- 添加knockback处理逻辑（ui.js）
- 修复ui.js中多余的代码
- 所有JS文件语法检查通过

### v=168（2026-08-28）
**更新内容**：
- 开始实施战斗画面和战斗节奏优化整改路线
- 检查当前战斗画面动画效果：确认已有攻击前冲、受击防御、立绘攻击动画、立绘防御动画、立绘呼吸动画、技能专属演出动作、技能演出时的屏幕闪光
- P0"让角色动起来"大部分内容已实现，待增强：命中瞬间hitstop、敌方击退knockback

### v=167（2026-08-28）
**更新内容**：
- 创建版本更新记录文档（`demo/docs/版本更新记录.md`）
- 记录从v=150到v=166的版本更新内容
- 建立版本更新规范：每次代码变更后必须递增版本号并在此文件中备注
- 回答用户问题：之前的版本更新没有在文档中备注，从v=167开始建立规范

### v=166（2026-08-28）
**更新内容**：
- P3架构还债阶段性检查：确认telemetry.js不存在，attr_calc.js被实际使用（game.js:291-293），不需要删除
- 开始检查battle_resource / dao_system / save_system的旧内联双胞胎分支

### v=165（2026-08-28）
**更新内容**：
- 战斗屏信息簇收敛分析完成：当前约23个信息簇，识别出重复信息（血条、劫印机制）
- 制定优化方案：移除顶部状态栏血条、移除劫印机制显示移到底部、合并层数和回合数
- 预计信息簇从23个减少到约18个
- 实施中遇到字符串匹配问题，待后续解决

### v=164（2026-08-28）
**更新内容**：
- 战斗屏信息簇收敛实施尝试：创建Node.js脚本修改ui.js
- 脚本运行失败：模板字符串中的`${hero.portrait}`被Node.js解析为变量引用
- 重新创建脚本使用普通字符串，但未找到顶部状态栏和底部操作栏
- 战斗屏信息簇收敛仍需更精确的方法实施

### v=163（2026-08-28）
**更新内容**：
- 战斗屏信息簇分析：读取ui.js中战斗屏渲染代码（3730-3887行）
- 识别出当前信息簇约23个：顶部状态栏6个、战斗层12个、UI交互层5个
- 识别出重复信息：血条在顶部状态栏和战斗层都有显示、劫印机制在战斗层和顶部状态栏都有显示

### v=162（2026-08-28）
**更新内容**：
- P2内容补完阶段性检查：确认地区10-17地图皮肤已实现（data.js:638-670）
- 确认9个数值兜底boss补阶段脚本已实现（enemies.js:638-710）
- 确认任务卡折叠进面板已实现（ui.js:998-1005, main.js:205-211）
- P2内容补完4/5完成，仅剩战斗屏信息簇收敛

### v=161（2026-08-28）
**更新内容**：
- 事件链chainId/chainStep状态机 + hero/dao过滤字段真正落地
- 添加链式事件示例：五行山·封印松动三步链式事件（chain_wuxing_1/2/3）
- 添加英雄专属事件示例：唐僧·袈裟失窃（hero: ['tangseng']）
- 添加道途专属事件示例：战道·修罗场（dao: ['战']）
- 所有JS文件语法检查通过

### v=160（2026-08-28）
**更新内容**：
- P2内容补完开始：检查events.js中事件链chainId/chainStep状态机和hero/dao过滤字段
- 确认框架已实现（events.js:22-37, 61-72），但实际事件数据未使用这些字段
- 开始创建链式事件示例和hero/dao过滤示例

### v=159（2026-08-28）
**更新内容**：
- P1产品生死线全部完成：识破加perfect/ok/miss三档判定
- perfect（完美，时机≤0.5秒）：免伤+1.2×双攻反击+3气势
- ok（成功，时机0.5-1.5秒）：免伤+0.8×双攻反击+2气势（原默认）
- miss（失败，时机>1.5秒或未按到）：无效果，正常受击
- 所有JS文件语法检查通过


### v=455（V8.58 六道选择界面重构）
**日期**：2026-09-07
**更新内容**：
- 取消第一难"顺命/争胜/避世"三分组布局，改为六道长横条直接展示（渡/缘/战/夺/隐/逆）
- 鼠标悬停六道选项时，弹出观音对该六道的提示文案（含六道特色、优劣势说明）
- 点击六道选项后，底部显示确认栏："✓ 确认选择此道" + "↺ 重新选择"两个按键
- 确认后执行该选项的 effect（善恶、装备、好感等），重新选择则取消选中返回选项列表
- 数据层：trials81.js 第一难 options 从三个姿态（各含2 subOptions）平铺为六个直接选项，新增 guanyinTip/longDesc 字段
- UI层：ui_modals_2.js 新增 _sixdaoSelectModal 方法，sceneModal 开头检测 sixdaoSelect 标记
- 事件层：main.js 新增 sixdao-pick/sixdao-confirm/sixdao-repick 三个事件处理
- 样式：style.css 新增 206 行六道直选样式（长横条、悬停提示、确认栏、移动端适配）
- 验证：regression 157/0 通过，浏览器 0 JS 错误，交互逻辑全部测试通过


### v=456（V8.59 战斗界面去框）
**日期**：2026-09-07
**更新内容**：
- 取消战斗时怪物的红框：移除 .fb-avatar.foe 的红色 box-shadow 外发光（rgba(150,40,40,.55) 3px spread）
- 取消战斗时英雄的虚框：移除 .fb-avatar 基础样式的金色半透明外发光（rgba(236,192,120,.22) 3px spread）
- 修改方式：.fb-avatar.sprite.you, .fb-avatar.sprite.foe 新增 box-shadow: none !important（V8.49 已重置 border/background，本次补全 box-shadow）
- 保留 filter: drop-shadow 投影效果（让立绘从背景中浮起，非框）
- 验证：浏览器实机确认 heroBoxShadow=none, foeBoxShadow=none

### v=457（V8.60 装备立绘尺寸适配手机横版）
**日期**：2026-09-07
**更新内容**：
- 问题：长安送行·观音赠宝弹窗中装备立绘（song-gift-icon）无 CSS 尺寸限制，按原始 1024×1024 像素显示，占据弹窗大部分空间，不符合手机横版大小
- 修复：style.css 新增 .song-gift-list / .song-gift-item / .song-gift-icon / .song-gift-desc 样式
  - .song-gift-icon: width/height/max-width/max-height 均限制为 48px，object-fit: contain，flex-shrink: 0
  - .song-gift-item: flex 横向布局（图标+名称+描述），gap 12px，背景半透明+边框
  - .song-gift-list: 纵向 flex 布局，gap 8px
- 验证：浏览器实机确认装备立绘 offsetWidth=48, offsetHeight=48（原始 naturalWidth=1024），弹窗布局正常，regression 157/0 通过

### v=458（V8.61 英雄立绘+装备描述修复）
**日期**：2026-09-07
**更新内容**：
- 问题1：初始英雄选择界面立绘展示不完整——.hero-portrait-wrap 的 aspect-ratio: 3/4 在 flex 子元素中未生效，实际高度只有 10-45px（应为 169px），导致人物只显示中间一小条
  - 修复：#startscreen.start-v2 .hero-portrait-wrap 新增 flex-shrink: 0 !important，防止 flex 布局压缩子元素高度
  - 验证：浏览器实机确认立绘尺寸 126×169px（之前 126×10~45px），人物完整展示
- 问题3：装备详情弹窗中属性显示格式混乱——stat(e) 函数中 set（套装名）前面多加了一个"·"，导致 join(' · ') 后出现"· ·取经人套"（两个连续的"·"）；sysTag（体/愿标签）与装备名称之间缺少空格
  - 修复1：ui_bag.js 中 set 从 `·${e.set}套` 改为 `${e.set}套`（去掉前面的"·"，由 join 统一添加）
  - 修复2：ui_bag.js 中装备列表项从 `${sysTag}<b>` 改为 `${sysTag} <b>`（添加空格分隔）
  - 验证：ui_bag.js 语法检查通过，regression 157/0 通过
- 问题2（刘洪立绘太大）：待进一步调查——当前战斗立绘使用 .fb-avatar.sprite（基础 108×132px，横屏 100×122px），Boss 立绘可能使用 .fb-foe-art（max 140×140px），需触发刘洪 Boss 战确认具体问题

### v=459–467（V8.62 战斗节奏与工程拆分）
**日期**：2026-09-08 ~ 2026-09-09
**更新内容**：
- `e760fa1` 动态节拍 + 倍速解禁（对标《杀戮尖塔》），游戏本体 game/ui 拆分层全量入库（v=467）
- 战斗节奏：普通命中顿帧、脉冲 outBack 回弹、三按键按下回弹、点空白快进尾拍
- 说明：本区间为工作区批量推进后一次性入库，459–466 未逐戳留独立提交，按聚合补登

### v=468–485（2026-09-09 本日多线程批量，含并行会话）
**日期**：2026-09-09
**更新内容**（按改动聚类；本日多线程并行推进，未逐戳留痕，此处补登至当前戳 v=485）：
- **战斗平衡**：B+ γ 幂律落地（`scale = clamp(raw^γ, 0.5, 1.5)`，γ=0.45 可热调）；新建 `scripts/validate_balance.js` 门禁（11/0）
- **战斗手感**：H6 伤害数字分层与暴击特写；H7 秩序/混沌祝福三选一 UI
- **短局模式 S1–S3**：`NDX.RunMode` 模块（data_runmode.js）；标题屏模式切换；终局判定；专属短局结算屏 `shortGameOver` + canvas 战绩分享卡；`scripts/validate_runmode.js` 门禁（35/0）
- **六道词汇**：方案 B——B 套佛经六道改名「六藏」，A 套道途独占六道词汇（`f0f464c`）
- **道途**：绝招道途加成接入真实伤害（`2a984a6`）；战道补偿——受击保留战意（`db78e4a`）
- **音频**：A 套六道 → 音景 key 映射补齐，修复试炼音景静默回落（`22cec5d`）
- **经文**：传承/土地庙面板键空间误配修正（`4d3806c`）；补齐 18 部未归经文，34 部全覆盖闭环（`247dd63`）
- **事件**：双线事件池 80 条逆道抉择善恶轴误配修正（`190da6f`）
- **存档**：移除 data_runmode 裸 localStorage 降级分支，收编至 `NDX.SaveSystem`（`ab930e2`）

### v=486（阶段一 · 收口）
**日期**：2026-09-10
**更新内容**（S1-1 ~ S1-8 机械收口，详见 `docs/逆道西行_全景综合分析与开发规划_v2终版_2026-09-09.md` §8.1）：
- **S1-1 气势爆发防御**：`combat_skills.js` 对越界 tier（>3 / 非整数 / NaN）钳位到 [1,3]，杜绝 `BURST_X[tier]` undefined → 伤害 NaN；`game_combat_2.js` 爆发日志改为调用前缓存档位，修复「一势/二势/三势」档位名恒空
- **S1-2 图标 404**：`ui_icons.js` 兜底映射 `generic_*.png` → `.webp`（6 个槽位）；新建 `_audit_s1_icons.js` + `_audit_s1_icons_runtime.js`，静态 780 条 + ICON_MAP 438 条全命中，404=0
- **S1-4 同族死键**：`game_core_2.js` 补 `addQuota('shop')`/`addQuota('event')`，修复 `_quotaSix` alt2 兜底路径 30 条永不可达；`data_quota.js` 补 shop/event 中文标签
- **S1-6 死法宝**：删除 `equipment.js` 中 3 处孤立 `cd: N,`（对象闭合后误写，污染 `NDX.TREASURES` 顶层键 `cd=1`）；3 件 red 级逆道真器（灭世黑莲/万毒蛊经/战神魔铠）接入逆道专属掉落口 `goldenBossTreasureFor`；法宝可达率 59/59 = 100%
- **S1-8 版本记录**：本文件补登至当前戳

### v=158及更早版本
**更新内容**：
- P0阻断修复全部完成：combat.js未定义变量修复、ui.js裸g→NDX.game、存档键名统一
- P1产品生死线：aiAutoTreasure仅在autoFight模式生效、pTurn.skillKind写入接通让连招可达
- 参照全面分析报告和整改意见书对游戏进行全面完善
- 具体更新内容请参考对话历史和代码变更记录

---

## 更新规范

1. **每次版本更新必须备注**：修改 `demo/js/*.js` 或 `demo/css/style.css` 后，必须递增 `demo/index.html` 中的 `?v=NN` 版本号，并在此文件中备注更新内容。
2. **更新内容格式**：版本号、日期、更新内容（分点列出）。
3. **关联文档**：重大更新需同步 `demo/docs/逆道西行_开发文档.md`。
4. **代码事实优先**：文档与代码冲突时以代码为准，并报告冲突。
