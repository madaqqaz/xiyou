# 《逆道西行》微信小游戏发布指引

> 对应 AGENTS.md §七「三端同步」与 §十一 验收门禁。本文档是微信小游戏端的发布/验收真源，与 `TapTap发布指引.md` 并列；核心玩法、数值、UI 布局必须与浏览器 H5 主线保持一致，平台差异只经适配层隔离。

## 一、现状（2026-08-29）

`demo/minigame/` 为微信小游戏适配宿主，已配置完毕：

```
minigame/
  game.js              # 入口 compileType=game：先 require adapter.js 再 require bundle.js
  adapter.js           # 浏览器 → 微信小游戏 API 适配（createCanvas / 触控 / 系统信息）
  bundle.js            # 打包产物：17 个逻辑层 + 2 个 Canvas 渲染引擎段（battle/ui）内联
  engine/              # Canvas 渲染引擎源（battle-canvas.js / ui-canvas.js；正式包忽略，已内联 bundle）
  dev.html             # 本地调试页（正式包忽略，packOptions.ignore）
  game.json            # deviceOrientation=landscape（横屏）
  project.config.json  # compileType="game"、专用小游戏 appid、es6
```

- **appid**：`minigame/project.config.json` 的 `appid`（专用小游戏 appid，非小程序的）。
- **渲染**：逻辑层复用浏览器 H5 核心，表现层用 Canvas 引擎重写（避免微信内嵌 web-view 白屏隐患）；`NDX.calcCombat` 输出的 `roundsDetail` 逐拍数据契约两端共用。
- **是否打包进正式包**：`packOptions.ignore` 忽略 `engine/` 与 `dev.html`（引擎已内联进 `bundle.js`）。

## 二、本地调试（新手可逐条照做）

### 2.1 首次准备
1. 官网下载并安装 **微信开发者工具**（稳定版；Windows 选 64 位）。启动后用你的微信扫码登录。
2. 需要一个**小游戏 AppID**（不是小程序的）：
   - 已有小游戏账号：直接用它。
   - 还没有：去微信公众平台（mp.weixin.qq.com）注册「小游戏」类目开户，拿到形如 `wx...` 的 AppID。
   - 本项目已配置的 appid 在 `minigame/project.config.json`，你也可以改用自己的 AppID（仅本地预览用自己/体验号都没问题）。

### 2.2 重建微信包（仅当核心逻辑变化后）
本地代码改动（`demo/js/*.js`）后，微信端依赖的 `bundle.js` 不会自动更新，需先重建：
```
node scripts/_build_minigame_bundle.js
```
- 脚本自带**一致性校验**：bundle 必须恰好覆盖 `index.html` 的脚本序、剔除纯 DOM 渲染层（`ui.js/main.js`）、并含最新核心特征串；任一不符会打印失败并中断。
- 生成物 `minigame/bundle.js` 不手改；`_*.js` 为生成脚本，不入库（AGENTS.md §五）。
- 重建后立刻跑 `node demo/scripts/platform_test.js` 验证三端对齐、无 bundle 漂移。

### 2.3 导入项目
1. 微信开发者工具 → **导入项目** → 目录选择 `demo/minigame/` 这一整个文件夹。
2. 会自动按 `project.config.json` 识别为 `compileType="game"`（小游戏类型）；AppID 填对的会直接给一个**小游戏**工程（不是小程序的选项卡）。
3. 首次导入若提示扑克牌/项目名，按提示确认即可。

### 2.4 模拟器调试
- 右侧"模拟器"窗口即所见即所得运行态；顶部可切换**iPhone/Android 机型**（重点测低端安卓：性能最差）。
- 调 `game.json` 已是 `deviceOrientation=landscape`，模拟器应自动横屏；若竖屏说明没选对 appid 或 config 被覆盖。
- **看报错**：底部/顶部「调试器」面板切到 **Console** 标签，这里显示 JS 异常、`console.log`、网络错误。前端报错通常会带文件名+行号（bundle.js:xxxx），能直接定位到源码段。

### 2.5 真机预览 / 真机调试（最重要）
- 工具栏「**预览**」：生成二维码，用**真机微信**扫码即可在手机上打开游戏——这是验证横屏锁定、触控手感、性能帧率的唯一可靠手段（模拟器≠真机）。
- 工具栏「**真机调试**」：扫码后真机出现 vConsole 浮标，可实时看 Console 报错、勾选网络/系统信息，适合排查真机才出现的白屏或性能问题。
- 真机排查点：横屏锁向、触控点按热区、Canvas 帧率（开发者工具左下角或真机 vConsole 可看 FPS）、退后台是否暂停。

### 2.6 本地浏览器快速预览（不依赖微信工具）
- 起本地服务器后直接开 `minigame/dev.html`：
  ```
  node scripts/_serve.cjs     # 绑定 0.0.0.0:8090（本地局域网内手机也可访问）
  ```
  浏览器打开 `http://localhost:8090/minigame/dev.html`，即可在桌面调试 Canvas 引擎渲染（`dev.html` 已排除出正式包，`packOptions.ignore`）。

### 2.7 命中"改了代码没效果"的排查顺序
1. 是否**重建了 bundle**？——核心逻辑改动必须走 2.2。
2. 是否核实 `index.html` 缓存版本号 `?v=NN` 已递增？——浏览器 H5 端尤其重要（AGENTS.md §五）。
3. 开发工具是否**重新编译**（Ctrl/Cmd+Shift+B 或点编译按钮）？是否清空了 IDE 缓存重载。

## 三、首包与分包（微信平台约束）

- **首包 ≤ 4MB**：当前 `bundle.js` 约 1.4MB，余量充足；但若后续引入大图/音频，须走 CDN 或微信云存储，不得打入首包。
- 新增美术/音频先评估体积 → 选项：CDN/云存储地址（适配层读远端）> 分包加载 > 压缩。
- `project.config.json` 已开 `es6`；`postcss:false`、`minified:false` 便于真机调试定位，正式发布可评估开 `minified`。

## 四、三端同步规则（AGENTS.md §七·五）

- **版本号同步**：微信小游戏与浏览器 H5 / TapTap 使用同一批 `index.html?=vNN` 缓存版本；`minigame/dev.html?v=` 单独同步。核心玩法更新必须三端同时递增、同时发布。
- **内容同步**：核心玩法更新不得某端抢先；平台专属功能（分享、成就、支付）经适配层隔离，不影响核心。
- **适配层隔离**：当前微信端用 `adapter.js` + Canvas 引擎承载；`NDX.Platform` 统一适配层仍属规划（AGENTS.md §七·五），接入后核心只调 `NDX.Platform`。

## 五、平台专属能力（走适配层）

| 能力 | 微信小游戏 | 说明 |
|---|---|---|
| 存档 | `wx.setStorageSync` / `getStorageSync` | 浏览器 `localStorage` 的微信替代，核心只依赖读写接口 |
| 恢复/分享 | 微信开放数据域 / 分享 API | 业务侧可选项，走适配层 |
| 无广告 | 无（纯单局+局外成就，不含广告位设计） | 与浏览器 H5 对齐 |

## 六、提审前移动端清单

- [ ] 横屏只读布局：`game.json` `deviceOrientation=landscape` 已设，真机确认为横屏锁向
- [ ] 触控点按区加大、字号可读（Canvas 引擎里确保按钮热区 ≥ 44px）
- [ ] 退后台暂停：`wx.onHide` 时暂停对局音效回合推进，避免断线/结算错位
- [ ] 首包体积 < 4MB
- [ ] 性能：真机帧率、Canvas drawImage 频率、GC 停顿——高端/低端机各测一遍
- [ ] `NDX.Platform` 适配层接入后跑 `node demo/scripts/platform_test.js`（三端兼容门禁）
- [ ] 本地验收命令（AGENTS.md §十一）运行并留证

## 七、审核合规

- 无违规内容：黑暗西游为成人化改编，需在提审说明中如实标注题材；不得含诱导分享、强立付费页。
- 存档与隐私：仅本地存档，无网络账号强制；若接入开放数据域，按微信隐私指引声明。

## 八、验收门禁（AGENTS.md §十一）

- `node demo/scripts/platform_test.js` → 覆盖宿主 C（微信）在 wx stub 下加载 `minigame/bundle.js`，断言 NDX 完整暴露、新系统与纯函数可用。
- `demo/scripts/_smoke_bundle_node.js`（生成/探针）→ 打包一致性冒烟（节点下 bundle 断言）。
- 微信开发者工具真机验收：横屏、触控、存档读写、退后台暂停、帧率。
- 三端同步发布前，浏览器 H5 手动实机 + TapTap 模拟器各一遍，确认无一方抢先。

## 九、发布与提审（上传 → 过审 → 上线）

发布前先跑完「二、本地调试」和「六、提审前移动端清单」，再用开发者工具走提审：

1. **确认版本号同步**：本次核心玩法更新已把 `index.html?=vNN` 递增到最新号，微信端包与浏览器 H5 / TapTap 用同一批版本号（AGENTS.md §七·五），三端同步发布，不得某端抢先。
2. **重建 bundle**（涉及核心逻辑时）：`node scripts/_build_minigame_bundle.js`，并跑 `platform_test.js` 确认无漂移。
3. 微信开发者工具 → 右上「**上传**」：填版本号（建议与 `index.html?v=` 对应）与备注，上传生成**体验版**。
4. **体验版自查**：在微信真机扫码打开体验版，再走一遍真机清单（横屏、触控、存档、退后台、帧率）。
5. **提交审核**：开发者工具 →「上传」后到微信公众平台（mp.weixin.qq.com）→ 版本管理 → 提交审核。勾选横屏、填写类目与描述，黑暗西游为成人化改编，**题材需如实标注**（§七）。
6. **过审后发布**：全量发布前再次确认三端版本号一致；如配置了分阶段发布按提示操作。
7. **发布后回归**：真机打开线上版确认能进游戏、存档读写正常，并记录本次发布的三端版本号与日期存档（防漂移）。

## 十、常见问题排查

| 现象 | 优先排查 |
|---|---|
| 白屏 / 页面空白 | ① `bundle.js` 是否重建（2.2）② 浏览器 Console 首行报错指向哪个文件 ③ AppID 是否被覆盖成小程序 ④ 确认打开的是 `minigame/` 目录 |
| Console 报 `bundle.js:xxxx` | 定位到源码段；若报未定义变量/函数，往往是因为 **bundle 与最新 `js/*.js` 不同步**，重建即愈 |
| 改了代码没变化 | 按 2.7 顺序：重建 bundle → `?v=` 递增 → 重新编译 → 清 IDE 缓存 |
| 出现 `?v=NN` 404 | 浏览器 H5 端用 `file://` 直接双击时按键隔离的兼容性问题；本地验收统一走 HTTP（`node scripts/_serve.cjs`，8090 端口） |
| 竖屏而非横屏 | `game.json` 是否 `deviceOrientation=landscape`；AppID 是否用小游戏（非小程序）导致配置不生效 |
| 首包超 4MB | 看 §三：大图/音频走 CDN、云存储或分包，不塞首包 |
| 按钮点击无反应/难点 | Canvas 引擎里按钮热区是否 ≥44px；真机触控与模拟器不同，以真机为准 |
| 真机报错但模拟器正常 | 用「真机调试」vConsole 看真机 Console；多为性能、网络、机型兼容问题 |
| 中文字符乱码 | 确保文件为 **UTF-8 无 BOM** 保存（本项目惯例）；曾出现编码问题用转义序列规避解析失败 |

## 十一、相关文件速查

- 微信宿主目录：`demo/minigame/`（入口 `game.js` → `adapter.js` → `bundle.js`）
- 构建脚本：`demo/scripts/_build_minigame_bundle.js`
- 本地服务器：`demo/scripts/_serve.cjs`（0.0.0.0:8090）
- 三端兼容门禁：`demo/scripts/platform_test.js`
- 调试预览页：`demo/minigame/dev.html`（仅调试，已排除出正式包）
- 项目配置：`demo/minigame/project.config.json`